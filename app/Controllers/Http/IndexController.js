'use strict'

class IndexController {
  async index({ response }) {
    response.send("Index Controller ok")


  }

  async index({ response }) {
    response.send("Index Controller ok")
  }
}

module.exports = IndexController
